# Persist exact versions on every `npm install <pkg>` — no semver ranges
save-exact=true

# Run audit after every install (warns on high/critical, does not fail)
audit=true

# Verify package integrity via lockfile — npm ci already does this, but this
# ensures plain `npm install` also refuses tampered packages
package-lock=true
